/// Gemini API 설정 예제 파일
///
/// ⚠️ 이 파일을 복사해서 gemini_config.dart로 이름을 변경하고 API 키를 입력하세요.
/// gemini_config.dart는 .gitignore에 추가되어 Git에 커밋되지 않습니다.
class GeminiConfig {
  // Google AI Studio에서 발급받은 API 키를 여기에 입력하세요
  // https://aistudio.google.com/app/apikey
  static const String apiKey = '여기에_발급받은_API_키_입력';

  // 사용할 모델 선택
  // - gemini-1.5-flash: 빠르고 무료 (추천)
  // - gemini-1.5-pro: 더 정확하지만 느림
  static const String model = 'gemini-1.5-flash';

  // 최대 출력 토큰 수 (응답 길이 제한)
  static const int maxOutputTokens = 1000;

  // 응답 온도 (0.0 ~ 2.0, 높을수록 창의적)
  static const double temperature = 0.7;
}

